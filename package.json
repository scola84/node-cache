{
  "name": "@scola/cache",
  "version": "0.0.0",
  "description": "Cache",
  "repository": "github:scola84/node-cache",
  "author": "M.C. van den Boogaart <scola@kvdb.name>",
  "license": "MIT",
  "main": "./dist/cache.js",
  "jsnext:main": "./index.js",
  "dependencies": {
    "async": "^2.1.4",
    "sha1": "^1.1.1"
  },
  "devDependencies": {
    "rollup": "^0.37.0",
    "rollup-plugin-buble": "^0.15.0",
    "uglify-js": "^2.7.5"
  },
  "scripts": {
    "build": "npm run cleanup && npm run rollup",
    "build:min": "npm run build && npm run min",
    "cleanup": "rm -rf dist",
    "min": "uglifyjs dist/cache.js -m -o dist/cache.min.js",
    "prepublish": "npm run build:min",
    "rollup": "rollup -c"
  }
}
